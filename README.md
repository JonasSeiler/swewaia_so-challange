# so-challenge

Initial Python project scaffold for milestone-oriented data workflows.

The project is configured for uv dependency management and organized to keep:
- data collection in `so_challenge/data_fetcher.py`
- visualization in `so_challenge/plotter.py`
- milestone definitions in `so_challenge/milestones.py`

Testing is set up with pytest under `tests/`.
